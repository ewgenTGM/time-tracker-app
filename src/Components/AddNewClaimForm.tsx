import React from 'react';
import {Grid} from '@mui/material';

export const AddNewClaimForm: React.FC = props => {
	return (
		<div>
			<Grid container justifyContent='center'>
				<h3>Здесь можно будет сделать заявку</h3>
			</Grid>
		</div>
	);
};